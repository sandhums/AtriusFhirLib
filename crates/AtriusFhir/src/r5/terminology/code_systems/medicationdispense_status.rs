// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: MedicationDispenseStatusCodes
///Canonical URL: http://hl7.org/fhir/CodeSystem/medicationdispense-status
///Version: 5.0.0
///Title: Medication Dispense Status Codes
///Status: active
///Medication Dispense Status Codes
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum MedicationDispenseStatusCodes {
    ///Display: Preparation
    ///Definition: The core event has not started yet, but some staging activities have begun (e.g. initial compounding or packaging of medication). Preparation stages may be tracked for billing purposes.
    #[serde(rename = "preparation")]
    Preparation,
    ///Display: In Progress
    ///Definition: The dispensed product is ready for pickup.
    #[serde(rename = "in-progress")]
    InProgress,
    ///Display: Cancelled
    ///Definition: The dispensed product was not and will never be picked up by the patient.
    #[serde(rename = "cancelled")]
    Cancelled,
    ///Display: On Hold
    ///Definition: The dispense process is paused while waiting for an external event to reactivate the dispense.  For example, new stock has arrived or the prescriber has called.
    #[serde(rename = "on-hold")]
    OnHold,
    ///Display: Completed
    ///Definition: The dispensed product has been picked up.
    #[serde(rename = "completed")]
    Completed,
    ///Display: Entered in Error
    ///Definition: The dispense was entered in error and therefore nullified.
    #[serde(rename = "entered-in-error")]
    EnteredInError,
    ///Display: Stopped
    ///Definition: Actions implied by the dispense have been permanently halted, before all of them occurred.
    #[serde(rename = "stopped")]
    Stopped,
    ///Display: Declined
    ///Definition: The dispense was declined and not performed.
    #[serde(rename = "declined")]
    Declined,
    ///Display: Unknown
    ///Definition: The authoring system does not know which of the status values applies for this medication dispense.  Note: this concept is not to be used for other - one of the listed statuses is presumed to apply, it's just now known which one.
    #[serde(rename = "unknown")]
    Unknown,
}
impl MedicationDispenseStatusCodes {
    pub fn as_code(&self) -> &'static str {
        match self {
            MedicationDispenseStatusCodes::Preparation => "preparation",
            MedicationDispenseStatusCodes::InProgress => "in-progress",
            MedicationDispenseStatusCodes::Cancelled => "cancelled",
            MedicationDispenseStatusCodes::OnHold => "on-hold",
            MedicationDispenseStatusCodes::Completed => "completed",
            MedicationDispenseStatusCodes::EnteredInError => "entered-in-error",
            MedicationDispenseStatusCodes::Stopped => "stopped",
            MedicationDispenseStatusCodes::Declined => "declined",
            MedicationDispenseStatusCodes::Unknown => "unknown",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/CodeSystem/medicationdispense-status"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for MedicationDispenseStatusCodes {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "preparation" => Ok(MedicationDispenseStatusCodes::Preparation),
            "in-progress" => Ok(MedicationDispenseStatusCodes::InProgress),
            "cancelled" => Ok(MedicationDispenseStatusCodes::Cancelled),
            "on-hold" => Ok(MedicationDispenseStatusCodes::OnHold),
            "completed" => Ok(MedicationDispenseStatusCodes::Completed),
            "entered-in-error" => Ok(MedicationDispenseStatusCodes::EnteredInError),
            "stopped" => Ok(MedicationDispenseStatusCodes::Stopped),
            "declined" => Ok(MedicationDispenseStatusCodes::Declined),
            "unknown" => Ok(MedicationDispenseStatusCodes::Unknown),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for MedicationDispenseStatusCodes {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

