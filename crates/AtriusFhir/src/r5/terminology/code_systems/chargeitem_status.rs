// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: ChargeItemStatus
///Canonical URL: http://hl7.org/fhir/chargeitem-status
///Version: 5.0.0
///Title: Charge Item Status
///Status: active
///Codes identifying the lifecycle stage of a ChargeItem.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum ChargeItemStatus {
    ///Display: Planned
    ///Definition: The charge item has been entered, but the charged service is not  yet complete, so it shall not be billed yet but might be used in the context of pre-authorization.
    #[serde(rename = "planned")]
    Planned,
    ///Display: Billable
    ///Definition: The charge item is ready for billing.
    #[serde(rename = "billable")]
    Billable,
    ///Display: Not billable
    ///Definition: The charge item has been determined to be not billable (e.g. due to rules associated with the billing code).
    #[serde(rename = "not-billable")]
    NotBillable,
    ///Display: Aborted
    ///Definition: The processing of the charge was aborted.
    #[serde(rename = "aborted")]
    Aborted,
    ///Display: Billed
    ///Definition: The charge item has been billed (e.g. a billing engine has generated financial transactions by applying the associated ruled for the charge item to the context of the Encounter, and placed them into Claims/Invoices.
    #[serde(rename = "billed")]
    Billed,
    ///Display: Entered in Error
    ///Definition: The charge item has been entered in error and should not be processed for billing.
    #[serde(rename = "entered-in-error")]
    EnteredInError,
    ///Display: Unknown
    ///Definition: The authoring system does not know which of the status values currently applies for this charge item  Note: This concept is not to be used for "other" - one of the listed statuses is presumed to apply, it's just not known which one.
    #[serde(rename = "unknown")]
    Unknown,
}
impl ChargeItemStatus {
    pub fn as_code(&self) -> &'static str {
        match self {
            ChargeItemStatus::Planned => "planned",
            ChargeItemStatus::Billable => "billable",
            ChargeItemStatus::NotBillable => "not-billable",
            ChargeItemStatus::Aborted => "aborted",
            ChargeItemStatus::Billed => "billed",
            ChargeItemStatus::EnteredInError => "entered-in-error",
            ChargeItemStatus::Unknown => "unknown",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/chargeitem-status"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for ChargeItemStatus {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "planned" => Ok(ChargeItemStatus::Planned),
            "billable" => Ok(ChargeItemStatus::Billable),
            "not-billable" => Ok(ChargeItemStatus::NotBillable),
            "aborted" => Ok(ChargeItemStatus::Aborted),
            "billed" => Ok(ChargeItemStatus::Billed),
            "entered-in-error" => Ok(ChargeItemStatus::EnteredInError),
            "unknown" => Ok(ChargeItemStatus::Unknown),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for ChargeItemStatus {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

