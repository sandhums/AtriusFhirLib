// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: ConsentState
///Canonical URL: http://hl7.org/fhir/consent-state-codes
///Version: 5.0.0
///Title: Consent State
///Status: active
///Indicates the state of the consent.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum ConsentState {
    ///Display: Pending
    ///Definition: The consent is in development or awaiting use but is not yet intended to be acted upon.
    #[serde(rename = "draft")]
    Draft,
    ///Display: Active
    ///Definition: The consent is to be followed and enforced.
    #[serde(rename = "active")]
    Active,
    ///Display: Inactive
    ///Definition: The consent is terminated or replaced.
    #[serde(rename = "inactive")]
    Inactive,
    ///Display: Abandoned
    ///Definition: The consent development has been terminated prior to completion.
    #[serde(rename = "not-done")]
    NotDone,
    ///Display: Entered in Error
    ///Definition: The consent was created wrongly (e.g. wrong patient) and should be ignored.
    #[serde(rename = "entered-in-error")]
    EnteredInError,
    ///Display: Unknown
    ///Definition: The resource is in an indeterminate state.
    #[serde(rename = "unknown")]
    Unknown,
}
impl ConsentState {
    pub fn as_code(&self) -> &'static str {
        match self {
            ConsentState::Draft => "draft",
            ConsentState::Active => "active",
            ConsentState::Inactive => "inactive",
            ConsentState::NotDone => "not-done",
            ConsentState::EnteredInError => "entered-in-error",
            ConsentState::Unknown => "unknown",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/consent-state-codes"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for ConsentState {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "draft" => Ok(ConsentState::Draft),
            "active" => Ok(ConsentState::Active),
            "inactive" => Ok(ConsentState::Inactive),
            "not-done" => Ok(ConsentState::NotDone),
            "entered-in-error" => Ok(ConsentState::EnteredInError),
            "unknown" => Ok(ConsentState::Unknown),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for ConsentState {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

