// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: ClaimAdjudicationDecisionCodes
///Canonical URL: http://hl7.org/fhir/claim-decision
///Version: 5.0.0
///Title: Claim Adjudication Decision Codes
///Status: active
///This value set provides Claim Adjudication Decision codes.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum ClaimAdjudicationDecisionCodes {
    ///Display: Denied
    ///Definition: The claim, or individual services, are not approved for any payment. This may also be known as 'rejected'.
    #[serde(rename = "denied")]
    Denied,
    ///Display: Approved
    ///Definition: The claim, or individual services, are approved as submitted.
    #[serde(rename = "approved")]
    Approved,
    ///Display: Partial
    ///Definition: The claim, or individual services, are approved at an amount less than as submitted.
    #[serde(rename = "partial")]
    Partial,
    ///Display: Pending
    ///Definition: The adjudication processing is not complete. This may be due to requiring manual review or receipt of additional information.
    #[serde(rename = "pending")]
    Pending,
}
impl ClaimAdjudicationDecisionCodes {
    pub fn as_code(&self) -> &'static str {
        match self {
            ClaimAdjudicationDecisionCodes::Denied => "denied",
            ClaimAdjudicationDecisionCodes::Approved => "approved",
            ClaimAdjudicationDecisionCodes::Partial => "partial",
            ClaimAdjudicationDecisionCodes::Pending => "pending",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/claim-decision"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for ClaimAdjudicationDecisionCodes {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "denied" => Ok(ClaimAdjudicationDecisionCodes::Denied),
            "approved" => Ok(ClaimAdjudicationDecisionCodes::Approved),
            "partial" => Ok(ClaimAdjudicationDecisionCodes::Partial),
            "pending" => Ok(ClaimAdjudicationDecisionCodes::Pending),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for ClaimAdjudicationDecisionCodes {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

