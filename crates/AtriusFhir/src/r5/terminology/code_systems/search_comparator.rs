// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: SearchComparator
///Canonical URL: http://hl7.org/fhir/search-comparator
///Version: 5.0.0
///Title: Search Comparator
///Status: active
///What Search Comparator Codes are supported in search.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum SearchComparator {
    ///Display: Equals
    ///Definition: the value for the parameter in the resource is equal to the provided value.
    #[serde(rename = "eq")]
    Eq,
    ///Display: Not Equals
    ///Definition: the value for the parameter in the resource is not equal to the provided value.
    #[serde(rename = "ne")]
    Ne,
    ///Display: Greater Than
    ///Definition: the value for the parameter in the resource is greater than the provided value.
    #[serde(rename = "gt")]
    Gt,
    ///Display: Less Than
    ///Definition: the value for the parameter in the resource is less than the provided value.
    #[serde(rename = "lt")]
    Lt,
    ///Display: Greater or Equals
    ///Definition: the value for the parameter in the resource is greater or equal to the provided value.
    #[serde(rename = "ge")]
    Ge,
    ///Display: Less of Equal
    ///Definition: the value for the parameter in the resource is less or equal to the provided value.
    #[serde(rename = "le")]
    Le,
    ///Display: Starts After
    ///Definition: the value for the parameter in the resource starts after the provided value.
    #[serde(rename = "sa")]
    Sa,
    ///Display: Ends Before
    ///Definition: the value for the parameter in the resource ends before the provided value.
    #[serde(rename = "eb")]
    Eb,
    ///Display: Approximately
    ///Definition: the value for the parameter in the resource is approximately the same to the provided value.
    #[serde(rename = "ap")]
    Ap,
}
impl SearchComparator {
    pub fn as_code(&self) -> &'static str {
        match self {
            SearchComparator::Eq => "eq",
            SearchComparator::Ne => "ne",
            SearchComparator::Gt => "gt",
            SearchComparator::Lt => "lt",
            SearchComparator::Ge => "ge",
            SearchComparator::Le => "le",
            SearchComparator::Sa => "sa",
            SearchComparator::Eb => "eb",
            SearchComparator::Ap => "ap",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/search-comparator"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for SearchComparator {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "eq" => Ok(SearchComparator::Eq),
            "ne" => Ok(SearchComparator::Ne),
            "gt" => Ok(SearchComparator::Gt),
            "lt" => Ok(SearchComparator::Lt),
            "ge" => Ok(SearchComparator::Ge),
            "le" => Ok(SearchComparator::Le),
            "sa" => Ok(SearchComparator::Sa),
            "eb" => Ok(SearchComparator::Eb),
            "ap" => Ok(SearchComparator::Ap),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for SearchComparator {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

