// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: UDIEntryType
///Canonical URL: http://hl7.org/fhir/udi-entry-type
///Version: 5.0.0
///Title: UDI Entry Type
///Status: active
///Codes to identify how UDI data was entered.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum UDIEntryType {
    ///Display: Barcode
    ///Definition: a barcodescanner captured the data from the device label.
    #[serde(rename = "barcode")]
    Barcode,
    ///Display: RFID
    ///Definition: An RFID chip reader captured the data from the device label.
    #[serde(rename = "rfid")]
    Rfid,
    ///Display: Manual
    ///Definition: The data was read from the label by a person and manually entered. (e.g.  via a keyboard).
    #[serde(rename = "manual")]
    Manual,
    ///Display: Card
    ///Definition: The data originated from a patient's implant card and was read by an operator.
    #[serde(rename = "card")]
    Card,
    ///Display: Self Reported
    ///Definition: The data originated from a patient source and was not directly scanned or read from a label or card.
    #[serde(rename = "self-reported")]
    SelfReported,
    ///Display: Electronic Transmission
    ///Definition: The UDI information was received electronically from the device through a communication protocol, such as the IEEE 11073 20601 version 4 exchange protocol over Bluetooth or USB.
    #[serde(rename = "electronic-transmission")]
    ElectronicTransmission,
    ///Display: Unknown
    ///Definition: The method of data capture has not been determined.
    #[serde(rename = "unknown")]
    Unknown,
}
impl UDIEntryType {
    pub fn as_code(&self) -> &'static str {
        match self {
            UDIEntryType::Barcode => "barcode",
            UDIEntryType::Rfid => "rfid",
            UDIEntryType::Manual => "manual",
            UDIEntryType::Card => "card",
            UDIEntryType::SelfReported => "self-reported",
            UDIEntryType::ElectronicTransmission => "electronic-transmission",
            UDIEntryType::Unknown => "unknown",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/udi-entry-type"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for UDIEntryType {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "barcode" => Ok(UDIEntryType::Barcode),
            "rfid" => Ok(UDIEntryType::Rfid),
            "manual" => Ok(UDIEntryType::Manual),
            "card" => Ok(UDIEntryType::Card),
            "self-reported" => Ok(UDIEntryType::SelfReported),
            "electronic-transmission" => Ok(UDIEntryType::ElectronicTransmission),
            "unknown" => Ok(UDIEntryType::Unknown),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for UDIEntryType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

