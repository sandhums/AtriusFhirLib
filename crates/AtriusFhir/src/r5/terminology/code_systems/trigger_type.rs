// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: TriggerType
///Canonical URL: http://hl7.org/fhir/trigger-type
///Version: 5.0.0
///Title: TriggerType
///Status: draft
///The type of trigger.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum TriggerType {
    ///Display: Named Event
    ///Definition: The trigger occurs in response to a specific named event, and no other information about the trigger is specified. Named events are completely pre-coordinated, and the formal semantics of the trigger are not provided.
    #[serde(rename = "named-event")]
    NamedEvent,
    ///Display: Periodic
    ///Definition: The trigger occurs at a specific time or periodically as described by a timing or schedule. A periodic event cannot have any data elements, but may have a name assigned as a shorthand for the event.
    #[serde(rename = "periodic")]
    Periodic,
    ///Display: Data Changed
    ///Definition: The trigger occurs whenever data of a particular type is changed in any way, either added, modified, or removed.
    #[serde(rename = "data-changed")]
    DataChanged,
    ///Display: Data Accessed
    ///Definition: The trigger occurs whenever data of a particular type is accessed.
    #[serde(rename = "data-accessed")]
    DataAccessed,
    ///Display: Data Access Ended
    ///Definition: The trigger occurs whenever access to data of a particular type is completed.
    #[serde(rename = "data-access-ended")]
    DataAccessEnded,
}
impl TriggerType {
    pub fn as_code(&self) -> &'static str {
        match self {
            TriggerType::NamedEvent => "named-event",
            TriggerType::Periodic => "periodic",
            TriggerType::DataChanged => "data-changed",
            TriggerType::DataAccessed => "data-accessed",
            TriggerType::DataAccessEnded => "data-access-ended",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/trigger-type"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for TriggerType {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "named-event" => Ok(TriggerType::NamedEvent),
            "periodic" => Ok(TriggerType::Periodic),
            "data-changed" => Ok(TriggerType::DataChanged),
            "data-accessed" => Ok(TriggerType::DataAccessed),
            "data-access-ended" => Ok(TriggerType::DataAccessEnded),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for TriggerType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

