// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: StructureMapSourceListMode
///Canonical URL: http://hl7.org/fhir/map-source-list-mode
///Version: 5.0.0
///Title: Structure Map Source List Mode
///Status: active
///If field is a list, how to manage the source.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum StructureMapSourceListMode {
    ///Display: First
    ///Definition: Only process this rule for the first in the list.
    #[serde(rename = "first")]
    First,
    ///Display: All but the first
    ///Definition: Process this rule for all but the first.
    #[serde(rename = "not_first")]
    NotFirst,
    ///Display: Last
    ///Definition: Only process this rule for the last in the list.
    #[serde(rename = "last")]
    Last,
    ///Display: All but the last
    ///Definition: Process this rule for all but the last.
    #[serde(rename = "not_last")]
    NotLast,
    ///Display: Enforce only one
    ///Definition: Only process this rule is there is only item.
    #[serde(rename = "only_one")]
    OnlyOne,
}
impl StructureMapSourceListMode {
    pub fn as_code(&self) -> &'static str {
        match self {
            StructureMapSourceListMode::First => "first",
            StructureMapSourceListMode::NotFirst => "not_first",
            StructureMapSourceListMode::Last => "last",
            StructureMapSourceListMode::NotLast => "not_last",
            StructureMapSourceListMode::OnlyOne => "only_one",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/map-source-list-mode"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for StructureMapSourceListMode {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "first" => Ok(StructureMapSourceListMode::First),
            "not_first" => Ok(StructureMapSourceListMode::NotFirst),
            "last" => Ok(StructureMapSourceListMode::Last),
            "not_last" => Ok(StructureMapSourceListMode::NotLast),
            "only_one" => Ok(StructureMapSourceListMode::OnlyOne),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for StructureMapSourceListMode {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

