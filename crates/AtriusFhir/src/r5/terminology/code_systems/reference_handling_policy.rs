// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: ReferenceHandlingPolicy
///Canonical URL: http://hl7.org/fhir/reference-handling-policy
///Version: 5.0.0
///Title: Reference Handling Policy
///Status: active
///A set of flags that defines how references are supported.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum ReferenceHandlingPolicy {
    ///Display: Literal References
    ///Definition: The server supports and populates Literal references (i.e. using Reference.reference) where they are known (this code does not guarantee that all references are literal; see 'enforced').
    #[serde(rename = "literal")]
    Literal,
    ///Display: Logical References
    ///Definition: The server allows logical references (i.e. using Reference.identifier).
    #[serde(rename = "logical")]
    Logical,
    ///Display: Resolves References
    ///Definition: The server will attempt to resolve logical references to literal references - i.e. converting Reference.identifier to Reference.reference (if resolution fails, the server may still accept resources; see logical).
    #[serde(rename = "resolves")]
    Resolves,
    ///Display: Reference Integrity Enforced
    ///Definition: The server enforces that references have integrity - e.g. it ensures that references can always be resolved. This is typically the case for clinical record systems, but often not the case for middleware/proxy systems.
    #[serde(rename = "enforced")]
    Enforced,
    ///Display: Local References Only
    ///Definition: The server does not support references that point to other servers.
    #[serde(rename = "local")]
    Local,
}
impl ReferenceHandlingPolicy {
    pub fn as_code(&self) -> &'static str {
        match self {
            ReferenceHandlingPolicy::Literal => "literal",
            ReferenceHandlingPolicy::Logical => "logical",
            ReferenceHandlingPolicy::Resolves => "resolves",
            ReferenceHandlingPolicy::Enforced => "enforced",
            ReferenceHandlingPolicy::Local => "local",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/reference-handling-policy"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for ReferenceHandlingPolicy {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "literal" => Ok(ReferenceHandlingPolicy::Literal),
            "logical" => Ok(ReferenceHandlingPolicy::Logical),
            "resolves" => Ok(ReferenceHandlingPolicy::Resolves),
            "enforced" => Ok(ReferenceHandlingPolicy::Enforced),
            "local" => Ok(ReferenceHandlingPolicy::Local),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for ReferenceHandlingPolicy {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

