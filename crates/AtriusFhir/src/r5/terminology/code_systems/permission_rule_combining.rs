// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: PermissionRuleCombining
///Canonical URL: http://hl7.org/fhir/permission-rule-combining
///Version: 5.0.0
///Title: Permission Rule Combining
///Status: active
///Codes identifying the rule combining. See XACML Combining algorithms  http://docs.oasis-open.org/xacml/3.0/xacml-3.0-core-spec-cos01-en.html
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum PermissionRuleCombining {
    ///Display: Deny-overrides
    ///Definition: The deny overrides combining algorithm is intended for those cases where a deny decision should have priority over a permit decision.
    #[serde(rename = "deny-overrides")]
    DenyOverrides,
    ///Display: Permit-overrides
    ///Definition: The permit overrides combining algorithm is intended for those cases where a permit decision should have priority over a deny decision.
    #[serde(rename = "permit-overrides")]
    PermitOverrides,
    ///Display: Ordered-deny-overrides
    ///Definition: The behavior of this algorithm is identical to that of the “Deny-overrides” rule-combining algorithm with one exception.  The order in which the collection of rules is evaluated SHALL match the order as listed in the permission.
    #[serde(rename = "ordered-deny-overrides")]
    OrderedDenyOverrides,
    ///Display: Ordered-permit-overrides
    ///Definition: The behavior of this algorithm is identical to that of the “Permit-overrides” rule-combining algorithm with one exception.  The order in which the collection of rules is evaluated SHALL match the order as listed in the permission.
    #[serde(rename = "ordered-permit-overrides")]
    OrderedPermitOverrides,
    ///Display: Deny-unless-permit
    ///Definition: The “Deny-unless-permit” combining algorithm is intended for those cases where a permit decision should have priority over a deny decision, and an “Indeterminate” or “NotApplicable” must never be the result. It is particularly useful at the top level in a policy structure to ensure that a PDP will always return a definite “Permit” or “Deny” result.
    #[serde(rename = "deny-unless-permit")]
    DenyUnlessPermit,
    ///Display: Permit-unless-deny
    ///Definition: The “Permit-unless-deny” combining algorithm is intended for those cases where a deny decision should have priority over a permit decision, and an “Indeterminate” or “NotApplicable” must never be the result. It is particularly useful at the top level in a policy structure to ensure that a PDP will always return a definite “Permit” or “Deny” result. This algorithm has the following behavior.
    #[serde(rename = "permit-unless-deny")]
    PermitUnlessDeny,
}
impl PermissionRuleCombining {
    pub fn as_code(&self) -> &'static str {
        match self {
            PermissionRuleCombining::DenyOverrides => "deny-overrides",
            PermissionRuleCombining::PermitOverrides => "permit-overrides",
            PermissionRuleCombining::OrderedDenyOverrides => "ordered-deny-overrides",
            PermissionRuleCombining::OrderedPermitOverrides => "ordered-permit-overrides",
            PermissionRuleCombining::DenyUnlessPermit => "deny-unless-permit",
            PermissionRuleCombining::PermitUnlessDeny => "permit-unless-deny",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/permission-rule-combining"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for PermissionRuleCombining {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "deny-overrides" => Ok(PermissionRuleCombining::DenyOverrides),
            "permit-overrides" => Ok(PermissionRuleCombining::PermitOverrides),
            "ordered-deny-overrides" => Ok(PermissionRuleCombining::OrderedDenyOverrides),
            "ordered-permit-overrides" => {
                Ok(PermissionRuleCombining::OrderedPermitOverrides)
            }
            "deny-unless-permit" => Ok(PermissionRuleCombining::DenyUnlessPermit),
            "permit-unless-deny" => Ok(PermissionRuleCombining::PermitUnlessDeny),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for PermissionRuleCombining {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

