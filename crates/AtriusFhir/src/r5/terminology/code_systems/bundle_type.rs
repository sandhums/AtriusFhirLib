// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: BundleType
///Canonical URL: http://hl7.org/fhir/bundle-type
///Version: 5.0.0
///Title: Bundle Type
///Status: active
///Indicates the purpose of a bundle - how it is intended to be used.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum BundleType {
    ///Display: Document
    ///Definition: The bundle is a document. The first resource is a Composition.
    #[serde(rename = "document")]
    Document,
    ///Display: Message
    ///Definition: The bundle is a message. The first resource is a MessageHeader.
    #[serde(rename = "message")]
    Message,
    ///Display: Transaction
    ///Definition: The bundle is a transaction - intended to be processed by a server as an atomic commit.
    #[serde(rename = "transaction")]
    Transaction,
    ///Display: Transaction Response
    ///Definition: The bundle is a transaction response. Because the response is a transaction response, the transaction has succeeded, and all responses are error free.
    #[serde(rename = "transaction-response")]
    TransactionResponse,
    ///Display: Batch
    ///Definition: The bundle is a set of actions - intended to be processed by a server as a group of independent actions.
    #[serde(rename = "batch")]
    Batch,
    ///Display: Batch Response
    ///Definition: The bundle is a batch response. Note that as a batch, some responses may indicate failure and others success.
    #[serde(rename = "batch-response")]
    BatchResponse,
    ///Display: History List
    ///Definition: The bundle is a list of resources from a history interaction on a server.
    #[serde(rename = "history")]
    History,
    ///Display: Search Results
    ///Definition: The bundle is a list of resources returned as a result of a search/query interaction, operation, or message.
    #[serde(rename = "searchset")]
    Searchset,
    ///Display: Collection
    ///Definition: The bundle is a set of resources collected into a single package for ease of distribution that imposes no processing obligations or behavioral rules beyond persistence.
    #[serde(rename = "collection")]
    Collection,
    ///Display: Subscription Notification
    ///Definition: The bundle has been generated by a Subscription to communicate information to a client.
    #[serde(rename = "subscription-notification")]
    SubscriptionNotification,
}
impl BundleType {
    pub fn as_code(&self) -> &'static str {
        match self {
            BundleType::Document => "document",
            BundleType::Message => "message",
            BundleType::Transaction => "transaction",
            BundleType::TransactionResponse => "transaction-response",
            BundleType::Batch => "batch",
            BundleType::BatchResponse => "batch-response",
            BundleType::History => "history",
            BundleType::Searchset => "searchset",
            BundleType::Collection => "collection",
            BundleType::SubscriptionNotification => "subscription-notification",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/bundle-type"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for BundleType {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "document" => Ok(BundleType::Document),
            "message" => Ok(BundleType::Message),
            "transaction" => Ok(BundleType::Transaction),
            "transaction-response" => Ok(BundleType::TransactionResponse),
            "batch" => Ok(BundleType::Batch),
            "batch-response" => Ok(BundleType::BatchResponse),
            "history" => Ok(BundleType::History),
            "searchset" => Ok(BundleType::Searchset),
            "collection" => Ok(BundleType::Collection),
            "subscription-notification" => Ok(BundleType::SubscriptionNotification),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for BundleType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

