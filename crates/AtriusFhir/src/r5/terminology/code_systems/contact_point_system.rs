// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: ContactPointSystem
///Canonical URL: http://hl7.org/fhir/contact-point-system
///Version: 5.0.0
///Title: ContactPointSystem
///Status: active
///Telecommunications form for contact point.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum ContactPointSystem {
    ///Display: Phone
    ///Definition: The value is a telephone number used for voice calls. Use of full international numbers starting with + is recommended to enable automatic dialing support but not required.
    #[serde(rename = "phone")]
    Phone,
    ///Display: Fax
    ///Definition: The value is a fax machine. Use of full international numbers starting with + is recommended to enable automatic dialing support but not required.
    #[serde(rename = "fax")]
    Fax,
    ///Display: Email
    ///Definition: The value is an email address.
    #[serde(rename = "email")]
    Email,
    ///Display: Pager
    ///Definition: The value is a pager number. These may be local pager numbers that are only usable on a particular pager system.
    #[serde(rename = "pager")]
    Pager,
    ///Display: URL
    ///Definition: A contact that is not a phone, fax, pager or email address and is expressed as a URL.  This is intended for various institutional or personal contacts including web sites, blogs, Skype, Twitter, Facebook, etc. Do not use for email addresses.
    #[serde(rename = "url")]
    Url,
    ///Display: SMS
    ///Definition: A contact that can be used for sending a sms message (e.g. mobile phones, some landlines).
    #[serde(rename = "sms")]
    Sms,
    ///Display: Other
    ///Definition: A contact that is not a phone, fax, page or email address and is not expressible as a URL.  E.g. Internal mail address.  This SHOULD NOT be used for contacts that are expressible as a URL (e.g. Skype, Twitter, Facebook, etc.)  Extensions may be used to distinguish "other" contact types.
    #[serde(rename = "other")]
    Other,
}
impl ContactPointSystem {
    pub fn as_code(&self) -> &'static str {
        match self {
            ContactPointSystem::Phone => "phone",
            ContactPointSystem::Fax => "fax",
            ContactPointSystem::Email => "email",
            ContactPointSystem::Pager => "pager",
            ContactPointSystem::Url => "url",
            ContactPointSystem::Sms => "sms",
            ContactPointSystem::Other => "other",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/contact-point-system"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for ContactPointSystem {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "phone" => Ok(ContactPointSystem::Phone),
            "fax" => Ok(ContactPointSystem::Fax),
            "email" => Ok(ContactPointSystem::Email),
            "pager" => Ok(ContactPointSystem::Pager),
            "url" => Ok(ContactPointSystem::Url),
            "sms" => Ok(ContactPointSystem::Sms),
            "other" => Ok(ContactPointSystem::Other),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for ContactPointSystem {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

