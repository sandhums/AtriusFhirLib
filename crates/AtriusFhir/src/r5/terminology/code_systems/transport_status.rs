// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: TransportStatus
///Canonical URL: http://hl7.org/fhir/transport-status
///Version: 5.0.0
///Title: Transport Status
///Status: active
///Status of transport.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum TransportStatus {
    ///Display: In Progress
    ///Definition: Transport has started but not completed.
    #[serde(rename = "in-progress")]
    InProgress,
    ///Display: Completed
    ///Definition: Transport has been completed.
    #[serde(rename = "completed")]
    Completed,
    ///Display: Abandoned
    ///Definition: Transport was started but not completed.
    #[serde(rename = "abandoned")]
    Abandoned,
    ///Display: Cancelled
    ///Definition: Transport was cancelled before started.
    #[serde(rename = "cancelled")]
    Cancelled,
    ///Display: Planned
    ///Definition: Planned transport that is not yet requested.
    #[serde(rename = "planned")]
    Planned,
    ///Display: Entered In Error
    ///Definition: This electronic record should never have existed, though it is possible that real-world decisions were based on it. (If real-world activity has occurred, the status should be "abandoned" rather than "entered-in-error".).
    #[serde(rename = "entered-in-error")]
    EnteredInError,
}
impl TransportStatus {
    pub fn as_code(&self) -> &'static str {
        match self {
            TransportStatus::InProgress => "in-progress",
            TransportStatus::Completed => "completed",
            TransportStatus::Abandoned => "abandoned",
            TransportStatus::Cancelled => "cancelled",
            TransportStatus::Planned => "planned",
            TransportStatus::EnteredInError => "entered-in-error",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/transport-status"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for TransportStatus {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "in-progress" => Ok(TransportStatus::InProgress),
            "completed" => Ok(TransportStatus::Completed),
            "abandoned" => Ok(TransportStatus::Abandoned),
            "cancelled" => Ok(TransportStatus::Cancelled),
            "planned" => Ok(TransportStatus::Planned),
            "entered-in-error" => Ok(TransportStatus::EnteredInError),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for TransportStatus {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

