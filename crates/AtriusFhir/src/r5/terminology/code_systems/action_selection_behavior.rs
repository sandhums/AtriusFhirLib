// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: ActionSelectionBehavior
///Canonical URL: http://hl7.org/fhir/action-selection-behavior
///Version: 5.0.0
///Title: Action Selection Behavior
///Status: active
///Defines selection behavior of a group.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum ActionSelectionBehavior {
    ///Display: Any
    ///Definition: Any number of the actions in the group may be chosen, from zero to all.
    #[serde(rename = "any")]
    Any,
    ///Display: All
    ///Definition: All the actions in the group must be selected as a single unit.
    #[serde(rename = "all")]
    All,
    ///Display: All Or None
    ///Definition: All the actions in the group are meant to be chosen as a single unit: either all must be selected by the end user, or none may be selected.
    #[serde(rename = "all-or-none")]
    AllOrNone,
    ///Display: Exactly One
    ///Definition: The end user must choose one and only one of the selectable actions in the group. The user SHALL NOT choose none of the actions in the group.
    #[serde(rename = "exactly-one")]
    ExactlyOne,
    ///Display: At Most One
    ///Definition: The end user may choose zero or at most one of the actions in the group.
    #[serde(rename = "at-most-one")]
    AtMostOne,
    ///Display: One Or More
    ///Definition: The end user must choose a minimum of one, and as many additional as desired.
    #[serde(rename = "one-or-more")]
    OneOrMore,
}
impl ActionSelectionBehavior {
    pub fn as_code(&self) -> &'static str {
        match self {
            ActionSelectionBehavior::Any => "any",
            ActionSelectionBehavior::All => "all",
            ActionSelectionBehavior::AllOrNone => "all-or-none",
            ActionSelectionBehavior::ExactlyOne => "exactly-one",
            ActionSelectionBehavior::AtMostOne => "at-most-one",
            ActionSelectionBehavior::OneOrMore => "one-or-more",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/action-selection-behavior"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for ActionSelectionBehavior {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "any" => Ok(ActionSelectionBehavior::Any),
            "all" => Ok(ActionSelectionBehavior::All),
            "all-or-none" => Ok(ActionSelectionBehavior::AllOrNone),
            "exactly-one" => Ok(ActionSelectionBehavior::ExactlyOne),
            "at-most-one" => Ok(ActionSelectionBehavior::AtMostOne),
            "one-or-more" => Ok(ActionSelectionBehavior::OneOrMore),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for ActionSelectionBehavior {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

