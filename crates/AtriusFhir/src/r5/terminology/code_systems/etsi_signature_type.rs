// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: ETSISignatureTypeCodes
///Canonical URL: http://uri.etsi.org/01903/v1.2.2
///Version: 5.0.0
///Title: ETSI Signature Type Codes
///Status: active
///The ETSI TS 101 733 V2.2.1 (2013-04) - Electronic Signatures and Infrastructures (ESI) - defines a set of Commitment Types (Purpose of Signature). ETSI TS 101 903 V1.2.2 defines vocabulary identifiers for these Commitment Types. Digital Signature Purposes, an indication of the reason an entity signs a document. This is included in the signed information and can be used when determining accountability for various actions concerning the document.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum ETSISignatureTypeCodes {
    ///Display: Proof of origin
    ///Definition: indicates that the signer recognizes to have created, approved and sent the signed data object.
    #[serde(rename = "ProofOfOrigin")]
    Proofoforigin,
    ///Display: Proof of receipt
    ///Definition: indicates that signer recognizes to have received the content of the signed data object.
    #[serde(rename = "ProofOfReceipt")]
    Proofofreceipt,
    ///Display: Proof of delivery
    ///Definition: indicates that the TSP providing that indication has delivered a signed data object in a local store accessible to the recipient of the signed data object.
    #[serde(rename = "ProofOfDelivery")]
    Proofofdelivery,
    ///Display: Proof of sender
    ///Definition: indicates that the entity providing that indication has sent the signed data object (but not necessarily created it).
    #[serde(rename = "ProofOfSender")]
    Proofofsender,
    ///Display: Proof of approval
    ///Definition: indicates that the signer has approved the content of the signed data object.
    #[serde(rename = "ProofOfapproval")]
    Proofofapproval,
    ///Display: Proof of creation
    ///Definition: indicates that the signer has created the signed data object (but not necessarily approved, nor sent it).
    #[serde(rename = "ProofOfCreation")]
    Proofofcreation,
}
impl ETSISignatureTypeCodes {
    pub fn as_code(&self) -> &'static str {
        match self {
            ETSISignatureTypeCodes::Proofoforigin => "ProofOfOrigin",
            ETSISignatureTypeCodes::Proofofreceipt => "ProofOfReceipt",
            ETSISignatureTypeCodes::Proofofdelivery => "ProofOfDelivery",
            ETSISignatureTypeCodes::Proofofsender => "ProofOfSender",
            ETSISignatureTypeCodes::Proofofapproval => "ProofOfapproval",
            ETSISignatureTypeCodes::Proofofcreation => "ProofOfCreation",
        }
    }
    pub fn system() -> &'static str {
        "http://uri.etsi.org/01903/v1.2.2"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for ETSISignatureTypeCodes {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "ProofOfOrigin" => Ok(ETSISignatureTypeCodes::Proofoforigin),
            "ProofOfReceipt" => Ok(ETSISignatureTypeCodes::Proofofreceipt),
            "ProofOfDelivery" => Ok(ETSISignatureTypeCodes::Proofofdelivery),
            "ProofOfSender" => Ok(ETSISignatureTypeCodes::Proofofsender),
            "ProofOfapproval" => Ok(ETSISignatureTypeCodes::Proofofapproval),
            "ProofOfCreation" => Ok(ETSISignatureTypeCodes::Proofofcreation),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for ETSISignatureTypeCodes {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

