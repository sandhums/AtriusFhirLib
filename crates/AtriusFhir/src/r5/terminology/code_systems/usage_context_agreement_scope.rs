// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: UsageContextAgreementScope
///Canonical URL: http://hl7.org/fhir/CodeSystem/usage-context-agreement-scope
///Version: 5.0.0
///Title: Usage Context Agreement Scope
///Status: draft
///This codesystem defines codes describing the type of agreement represented by an artifact, for example for use in CanonicalResource.usageContext.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum UsageContextAgreementScope {
    ///Display: Realm Base
    ///Definition: Defines use-case independent foundational expectations for exchange within a particular country or jurisdiction.  Should be accompanied by a jurisdiction.  Commonly used as a 'base' for more restrictive artifacts.
    #[serde(rename = "realm-base")]
    RealmBase,
    ///Display: Knowledge
    ///Definition: Defines use-case-specific information or guidance that is relevant to a specific business or health domain but is not mandated for particular use.
    #[serde(rename = "knowledge")]
    Knowledge,
    ///Display: Domain
    ///Definition: Defines use-case-specific requirements for a specific business or health domain.  May vary in jurisdictional scope from international to small region and in business scope from broad to narrow.
    #[serde(rename = "domain")]
    Domain,
    ///Display: Community
    ///Definition: Sets contractual or business expectations for systems participating in a particular exchange community.
    #[serde(rename = "community")]
    Community,
    ///Display: System Design
    ///Definition: Documents the specific capabilities of a single system as 'available' for purchase or use.  May have some variability reflecting options that can be configured.
    #[serde(rename = "system-design")]
    SystemDesign,
    ///Display: System Implementation
    ///Definition: Documents the specific points of a single production system or endpoint.  This may be time-specific - i.e. reflecting the system 'as it is now' or 'as it was at some point in the past'.
    #[serde(rename = "system-implementation")]
    SystemImplementation,
}
impl UsageContextAgreementScope {
    pub fn as_code(&self) -> &'static str {
        match self {
            UsageContextAgreementScope::RealmBase => "realm-base",
            UsageContextAgreementScope::Knowledge => "knowledge",
            UsageContextAgreementScope::Domain => "domain",
            UsageContextAgreementScope::Community => "community",
            UsageContextAgreementScope::SystemDesign => "system-design",
            UsageContextAgreementScope::SystemImplementation => "system-implementation",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/CodeSystem/usage-context-agreement-scope"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for UsageContextAgreementScope {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "realm-base" => Ok(UsageContextAgreementScope::RealmBase),
            "knowledge" => Ok(UsageContextAgreementScope::Knowledge),
            "domain" => Ok(UsageContextAgreementScope::Domain),
            "community" => Ok(UsageContextAgreementScope::Community),
            "system-design" => Ok(UsageContextAgreementScope::SystemDesign),
            "system-implementation" => {
                Ok(UsageContextAgreementScope::SystemImplementation)
            }
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for UsageContextAgreementScope {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

