// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem (non-enumerated): ColorRGB
///Canonical URL: http://hl7.org/fhir/color-rgb
///Version: 5.0.0
///Title: RGB Values for Color
///Status: active
///RGB values for color. Valid codes are #RRGGBB where RR, GG, and BB are two letter hex codes, values 00..ff. codes are case insensitive. This code system does not define the 16million+ valid RGB codes (terminology servers will need code to support the RGB grammar).
///NOTE: This CodeSystem does not enumerate concepts in valuesets.json (e.g. content=not-present or empty concept list).
///A full terminology server is required to validate membership for arbitrary codes.
pub struct ColorRGB;
impl ColorRGB {
    pub const URL: &'static str = "http://hl7.org/fhir/color-rgb";
    pub fn system() -> &'static str {
        Self::URL
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Best-effort local validation.
    ///
    /// Returns `true` only for systems where we can validate by grammar locally.
    /// Otherwise returns `false` (unknown locally).
    pub fn is_valid_code(code: &str) -> bool {
        if code.len() != 7 {
            return false;
        }
        let bytes = code.as_bytes();
        if bytes[0] != b'#' {
            return false;
        }
        code[1..]
            .bytes()
            .all(|b| matches!(b, b'0'..= b'9' | b'a'..= b'f' | b'A'..= b'F'))
    }
}

