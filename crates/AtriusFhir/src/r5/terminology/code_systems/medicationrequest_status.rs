// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: MedicationRequestStatusCodes
///Canonical URL: http://hl7.org/fhir/CodeSystem/medicationrequest-status
///Version: 5.0.0
///Title: MedicationRequest Status Codes
///Status: active
///MedicationRequest Status Codes
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum MedicationRequestStatusCodes {
    ///Display: Active
    ///Definition: The request is 'actionable', but not all actions that are implied by it have occurred yet.
    #[serde(rename = "active")]
    Active,
    ///Display: On Hold
    ///Definition: Actions implied by the request are to be temporarily halted. The request might or might not be resumed. May also be called 'suspended'.
    #[serde(rename = "on-hold")]
    OnHold,
    ///Display: Ended
    ///Definition: The request is no longer active and the subject should no longer be taking the medication.
    #[serde(rename = "ended")]
    Ended,
    ///Display: Entered in Error
    ///Definition: The request was recorded against the wrong patient or for some reason should not have been recorded (e.g. wrong medication, wrong dose, etc.). Some of the actions that are implied by the medication request may have occurred. For example, the medication may have been dispensed and the patient may have taken some of the medication.
    #[serde(rename = "entered-in-error")]
    EnteredInError,
    ///Display: Draft
    ///Definition: The request is not yet 'actionable', e.g. it is a work in progress, requires sign-off, verification or needs to be run through decision support process.
    #[serde(rename = "draft")]
    Draft,
    ///Display: Unknown
    ///Definition: The authoring/source system does not know which of the status values currently applies for this request. Note: This concept is not to be used for 'other' - one of the listed statuses is presumed to apply, but the authoring/source system does not know which.
    #[serde(rename = "unknown")]
    Unknown,
}
impl MedicationRequestStatusCodes {
    pub fn as_code(&self) -> &'static str {
        match self {
            MedicationRequestStatusCodes::Active => "active",
            MedicationRequestStatusCodes::OnHold => "on-hold",
            MedicationRequestStatusCodes::Ended => "ended",
            MedicationRequestStatusCodes::EnteredInError => "entered-in-error",
            MedicationRequestStatusCodes::Draft => "draft",
            MedicationRequestStatusCodes::Unknown => "unknown",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/CodeSystem/medicationrequest-status"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for MedicationRequestStatusCodes {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "active" => Ok(MedicationRequestStatusCodes::Active),
            "on-hold" => Ok(MedicationRequestStatusCodes::OnHold),
            "ended" => Ok(MedicationRequestStatusCodes::Ended),
            "entered-in-error" => Ok(MedicationRequestStatusCodes::EnteredInError),
            "draft" => Ok(MedicationRequestStatusCodes::Draft),
            "unknown" => Ok(MedicationRequestStatusCodes::Unknown),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for MedicationRequestStatusCodes {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

