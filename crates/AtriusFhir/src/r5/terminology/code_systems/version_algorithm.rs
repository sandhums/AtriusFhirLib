// @generated by AtriusValueSetGen
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use serde::{Deserialize, Serialize};

use super::super::super::{Boolean, Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR CodeSystem: VersionAlgorithm
///Canonical URL: http://hl7.org/fhir/version-algorithm
///Version: 5.0.0
///Title: Version Algorithm
///Status: active
///Indicates the mechanism used to compare versions to determine which is more current.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum VersionAlgorithm {
    ///Display: SemVer
    ///Definition: Uses the semantic versioning scheme as defined in [semver.org](http://semver.org).
    #[serde(rename = "semver")]
    Semver,
    ///Display: Integer
    ///Definition: Versions are integers and ordered numerically
    #[serde(rename = "integer")]
    Integer,
    ///Display: Alphabetical
    ///Definition: Simple alphabetic sort on a case-insensitive and accent-insensitive basis.  (Sorting of different cases or accented versions of a character is indeterminate)
    #[serde(rename = "alpha")]
    Alpha,
    ///Display: Date
    ///Definition: Versions are expressed as an ISO date/time syntax (including syntaxes with only portions of a date)
    #[serde(rename = "date")]
    Date,
    ///Display: Natural
    ///Definition: Sorted according to the algorithm defined here: [naturalordersort.org](http://www.naturalordersort.org/)
    #[serde(rename = "natural")]
    Natural,
}
impl VersionAlgorithm {
    pub fn as_code(&self) -> &'static str {
        match self {
            VersionAlgorithm::Semver => "semver",
            VersionAlgorithm::Integer => "integer",
            VersionAlgorithm::Alpha => "alpha",
            VersionAlgorithm::Date => "date",
            VersionAlgorithm::Natural => "natural",
        }
    }
    pub fn system() -> &'static str {
        "http://hl7.org/fhir/version-algorithm"
    }
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    /// Convert this code into a FHIR `code` primitive (`Code = Element<String, Extension>`).
    ///
    /// Useful for elements like `Patient.gender` that are bound directly to a ValueSet and use
    /// the `code` datatype.
    pub fn to_code(self) -> Code {
        Element {
            id: None,
            extension: None,
            value: Some(self.as_code().to_string()),
        }
    }
    /// Convert this code into a FHIR `Coding` with `system` and (if available) `version` set.
    ///
    /// Useful for `Coding` fields and for inclusion inside `CodeableConcept.coding`.
    pub fn to_coding(self) -> Coding {
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(Self::system().to_string()),
        };
        Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(self.to_code()),
            display: None,
            user_selected: Option::<Boolean>::None,
        }
    }
    /// Convert this code into a minimal `CodeableConcept` (with a single `coding`).
    ///
    /// This is especially useful for bound elements that use `CodeableConcept` (e.g.
    /// extensible bindings).
    pub fn to_codeable_concept(self) -> CodeableConcept {
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![self.to_coding()]),
            text: None,
        }
    }
    /// Parse a code string into this enum.
    ///
    /// This is a convenience wrapper around the `TryFrom<&str>` implementation.
    pub fn try_from_code(code: &str) -> Result<Self, ()> {
        <Self as core::convert::TryFrom<&str>>::try_from(code)
    }
    /// Parse from a FHIR `code` primitive (`Code = Element<String, Extension>`).
    pub fn try_from_code_element(code: &Code) -> Result<Self, ()> {
        match code.value.as_deref() {
            Some(v) => Self::try_from_code(v),
            None => Err(()),
        }
    }
}
impl core::convert::TryFrom<&str> for VersionAlgorithm {
    type Error = ();
    fn try_from(s: &str) -> Result<Self, <Self as core::convert::TryFrom<&str>>::Error> {
        match s {
            "semver" => Ok(VersionAlgorithm::Semver),
            "integer" => Ok(VersionAlgorithm::Integer),
            "alpha" => Ok(VersionAlgorithm::Alpha),
            "date" => Ok(VersionAlgorithm::Date),
            "natural" => Ok(VersionAlgorithm::Natural),
            _ => Err(()),
        }
    }
}
impl core::fmt::Display for VersionAlgorithm {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.as_code())
    }
}

