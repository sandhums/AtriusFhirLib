// @generated by AtriusValueSetGenerator
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use super::super::super::{Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR ValueSet: CitationArtifactClassifier
///Canonical URL: http://hl7.org/fhir/ValueSet/citation-artifact-classifier
///Version: 5.0.0
///Title: Citation Artifact Classifier
///Status: draft
///Citation artifact classifier
///Compose includes 31 explicit concept codes
///Includes systems:
///- http://hl7.org/fhir/citation-artifact-classifier
pub struct CitationArtifactClassifier;
impl CitationArtifactClassifier {
    pub const URL: &'static str = "http://hl7.org/fhir/ValueSet/citation-artifact-classifier";
    pub const HAS_NONLOCAL_RULES: bool = false;
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    pub fn include_systems() -> &'static [&'static str] {
        &["http://hl7.org/fhir/citation-artifact-classifier"]
    }
    pub fn include_value_sets() -> &'static [&'static str] {
        &[]
    }
    /// Systems that are included as whole CodeSystems but are not locally enumerable.
    ///
    /// If this is non-empty, callers should use a terminology server for definitive validation.
    pub fn include_whole_systems() -> &'static [&'static str] {
        &[]
    }
    /// Returns true only when this ValueSet can be treated as fully locally checkable.
    ///
    /// If `HAS_NONLOCAL_RULES` is true (filters, include.valueSet, or non-enumerated whole-system includes),
    /// local checks are best-effort and may require a terminology server.
    pub fn is_locally_enumerated() -> bool {
        !Self::HAS_NONLOCAL_RULES
            && (!Self::expansion_pairs().is_empty() || !Self::include_pairs().is_empty())
    }
    fn include_pairs() -> &'static [(&'static str, &'static str)] {
        &[
            ("http://hl7.org/fhir/citation-artifact-classifier", "audio"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "D001877"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "cds-artifact"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "D016420"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "common-share"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "D019991"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "D064886"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "dataset-unpublished"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "Electronic"),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "Electronic-eCollection",
            ),
            ("http://hl7.org/fhir/citation-artifact-classifier", "Electronic-Print"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "executable-app"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "fhir-resource"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "image"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "interactive-form"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "D016428"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "D016422"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "machine-code"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "medline-base"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "prediction-model"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "D000076942"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "Print"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "Print-Electronic"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "project-specific"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "protocol"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "pseudocode"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "D016425"),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "standard-specification",
            ),
            ("http://hl7.org/fhir/citation-artifact-classifier", "terminology"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "D059040"),
            ("http://hl7.org/fhir/citation-artifact-classifier", "webpage"),
        ]
    }
    fn include_entries() -> &'static [(
        &'static str,
        &'static str,
        Option<&'static str>,
        Option<&'static str>,
    )] {
        &[
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "D000076942",
                Some("Preprint"),
                Some("Scientific manuscript made available prior to PEER REVIEW."),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "D001877",
                Some("Book"),
                Some(
                    "Non-periodical written or printed works consisting of sheets of pages fastened or bound together within covers.",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "D016420",
                Some("Comment"),
                Some("Comment"),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "D016422",
                Some("Letter"),
                Some("Letter"),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "D016425",
                Some("Published Erratum"),
                Some("Published Erratum"),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "D016428",
                Some("Journal Article"),
                Some("Journal Article"),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "D019991",
                Some("Database"),
                Some(
                    "A structured file of information or a set of logically related data stored and retrieved using computer-based means.",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "D059040",
                Some("Video-Audio Media"),
                Some(
                    "Used with articles which include video files or clips, or for articles which are entirely video.",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "D064886",
                Some("Dataset"),
                Some(
                    "Works consisting of organized collections of data, which have been stored permanently in a formalized manner suitable for communication, interpretation, or processing.",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "Electronic",
                Some("Electronic"),
                Some("the journal is published in electronic format only"),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "Electronic-Print",
                Some("Electronic-Print"),
                Some(
                    "the journal is published first in electronic format followed by print (this value is currently used for just one journal, Nucleic Acids Research)",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "Electronic-eCollection",
                Some("Electronic-eCollection"),
                Some(
                    "used for electronic-only journals that publish individual articles first and then later collect them into an \"issue\" date that is typically called an eCollection.",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "Print",
                Some("Print"),
                Some("the journal is published in print format only"),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "Print-Electronic",
                Some("Print Electronic"),
                Some("the journal is published in both print and electronic format"),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "audio",
                Some("Audio file"),
                Some("The article cited is an audio file."),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "cds-artifact",
                Some("Clinical Decision Support Artifact"),
                Some(
                    "The artifact is used for decision support for healthcare decisions.",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "common-share",
                Some("Common Share"),
                Some(
                    "Citation Resource containing value added data that is openly shared",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "dataset-unpublished",
                Some("Dataset Unpublished"),
                Some(
                    "An organized collection of data that is not stored permanently for communication.",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "executable-app",
                Some("Executable app"),
                Some("Executable app"),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "fhir-resource",
                Some("FHIR Resource"),
                Some("The article cited is a FHIR resource."),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "image",
                Some("Image file"),
                Some("The article cited is an image file."),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "interactive-form",
                Some("Interactive Form"),
                Some("A user interface that supports data entry and data display."),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "machine-code",
                Some("Machine code"),
                Some("The article cited is machine code."),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "medline-base",
                Some("Medline Base"),
                Some("Citation Resource containing only data from Medline"),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "prediction-model",
                Some("Prediction Model"),
                Some(
                    "A formula or expression used to calculate an outcome representing a predicted result.",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "project-specific",
                Some("Project Specific"),
                Some(
                    "Citation Resource containing value added data specific to a project",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "protocol",
                Some("Protocol"),
                Some(
                    "The article cited is the protocol of an activity and not the results or findings.",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "pseudocode",
                Some("PseudoCode"),
                Some("A non-executable, human-readable representation of software code."),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "standard-specification",
                Some("Standard Specification"),
                Some(
                    "An explicit set of requirements for an item, material, component, system or service, often used to define a technical standard which is an established norm or requirement for a repeatable technical task.",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "terminology",
                Some("Terminology"),
                Some(
                    "A structured set of codes and display values, which may be subtyped as a code system, value set, taxonomy, or ontology.",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-artifact-classifier",
                "webpage",
                Some("Webpage"),
                Some("Webpage"),
            ),
        ]
    }
    /// Return the preferred display text for a (system, code) pair, if present in this ValueSet's explicit concepts.
    pub fn display(system: &str, code: &str) -> Option<&'static str> {
        for (s, c, d, _def) in Self::include_entries() {
            if *s == system && *c == code {
                return *d;
            }
        }
        None
    }
    /// Return the ValueSet concept definition for a (system, code) pair, if present.
    ///
    /// In `valuesets.json` this often comes from the extension:
    /// `http://hl7.org/fhir/StructureDefinition/valueset-concept-definition`.
    pub fn definition(system: &str, code: &str) -> Option<&'static str> {
        for (s, c, _d, def) in Self::include_entries() {
            if *s == system && *c == code {
                return *def;
            }
        }
        None
    }
    /// Create a minimal `Coding` from a (system, code) pair.
    ///
    /// If this ValueSet provides an explicit display for the code, it will be populated.
    pub fn to_coding(system: &str, code: &str) -> Coding {
        let sys_uri: Uri = Element {
            id: None,
            extension: None,
            value: Some(system.to_string()),
        };
        let code_el: Code = Element {
            id: None,
            extension: None,
            value: Some(code.to_string()),
        };
        let display = Self::display(system, code)
            .map(|s| FhirString {
                id: None,
                extension: None,
                value: Some(s.to_string()),
            });
        Coding {
            id: None,
            extension: None,
            system: Some(sys_uri),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(code_el),
            display,
            user_selected: None,
        }
    }
    fn exclude_pairs() -> &'static [(&'static str, &'static str)] {
        &[]
    }
    fn expansion_pairs() -> &'static [(&'static str, &'static str)] {
        &[]
    }
    /// Best-effort local validation for a `Coding` against this ValueSet.
    ///
    /// Rules:
    /// - Explicit excludes (compose.exclude.concept) always deny.
    /// - If expansion.contains exists, it is treated as an explicit allow-list.
    /// - Else if compose.include.concept exists, it is treated as an explicit allow-list.
    /// - Else: fall back to whole-system validation when possible (generated CodeSystem enums / RGB grammar).
    ///
    /// Note: terminology filters and include.valueSet references are NOT evaluated locally.
    pub fn contains_coding(coding: &Coding) -> bool {
        let system = match &coding.system {
            Some(s) => s.value.as_deref().unwrap_or(""),
            None => return false,
        };
        let code = match &coding.code {
            Some(c) => c.value.as_deref().unwrap_or(""),
            None => return false,
        };
        if Self::exclude_pairs().iter().any(|(s, c)| *s == system && *c == code) {
            return false;
        }
        let exp = Self::expansion_pairs();
        if !exp.is_empty() {
            return exp.iter().any(|(s, c)| *s == system && *c == code);
        }
        let inc = Self::include_pairs();
        if !inc.is_empty() {
            return inc.iter().any(|(s, c)| *s == system && *c == code);
        }
        match system {
            "http://hl7.org/fhir/citation-artifact-classifier" => {
                super::super::code_systems::citation_artifact_classifier::CitationArtifactClassifier::try_from_code(
                        code,
                    )
                    .is_ok()
            }
            _ => false,
        }
    }
    /// Best-effort local validation for a `CodeableConcept` against this ValueSet.
    ///
    /// Returns true if any `coding` entry is contained in the ValueSet.
    pub fn contains_codeable_concept(cc: &CodeableConcept) -> bool {
        match cc.coding.as_ref() {
            Some(codings) => codings.iter().any(|c| Self::contains_coding(c)),
            None => false,
        }
    }
    /// Convenience: validate a FHIR `code` primitive when the binding is on `code`.
    ///
    /// This only works reliably when the ValueSet includes exactly one system.
    pub fn contains_code(code: &Code) -> bool {
        let code_str = match code.value.as_deref() {
            Some(v) => v,
            None => return false,
        };
        let systems = Self::include_systems();
        if systems.len() != 1 {
            return false;
        }
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(systems[0].to_string()),
        };
        let code: Code = Element {
            id: None,
            extension: None,
            value: Some(code_str.to_string()),
        };
        let coding = Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: None,
            code: Some(code),
            display: None,
            user_selected: None,
        };
        Self::contains_coding(&coding)
    }
    /// Create a minimal `CodeableConcept` (single coding) from a (system, code) pair.
    pub fn to_codeable_concept(system: &str, code: &str) -> CodeableConcept {
        let coding = Self::to_coding(system, code);
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: None,
        }
    }
}
impl super::super::bindings::ValueSetMembership<Code> for CitationArtifactClassifier {
    fn contains(v: &Code) -> bool {
        Self::contains_code(v)
    }
}
impl super::super::bindings::ValueSetMembership<Coding> for CitationArtifactClassifier {
    fn contains(v: &Coding) -> bool {
        Self::contains_coding(v)
    }
}
impl super::super::bindings::ValueSetMembership<CodeableConcept>
for CitationArtifactClassifier {
    fn contains(v: &CodeableConcept) -> bool {
        Self::contains_codeable_concept(v)
    }
}
fn is_rgb_hex(code: &str) -> bool {
    let b = code.as_bytes();
    if b.len() != 7 || b[0] != b'#' {
        return false;
    }
    fn is_hex(x: u8) -> bool {
        (b'0'..=b'9').contains(&x) || (b'a'..=b'f').contains(&x)
            || (b'A'..=b'F').contains(&x)
    }
    is_hex(b[1]) && is_hex(b[2]) && is_hex(b[3]) && is_hex(b[4]) && is_hex(b[5])
        && is_hex(b[6])
}

