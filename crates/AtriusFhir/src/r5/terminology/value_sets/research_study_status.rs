// @generated by AtriusValueSetGenerator
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use super::super::super::{Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR ValueSet: ResearchStudyStatus
///Canonical URL: http://hl7.org/fhir/ValueSet/research-study-status
///Version: 5.0.0
///Title: Research Study Status
///Status: draft
///Codes that convey the current status of the research study.
///Compose includes 17 explicit concept codes
///Includes systems:
///- http://hl7.org/fhir/research-study-status
pub struct ResearchStudyStatus;
impl ResearchStudyStatus {
    pub const URL: &'static str = "http://hl7.org/fhir/ValueSet/research-study-status";
    pub const HAS_NONLOCAL_RULES: bool = false;
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    pub fn include_systems() -> &'static [&'static str] {
        &["http://hl7.org/fhir/research-study-status"]
    }
    pub fn include_value_sets() -> &'static [&'static str] {
        &[]
    }
    /// Systems that are included as whole CodeSystems but are not locally enumerable.
    ///
    /// If this is non-empty, callers should use a terminology server for definitive validation.
    pub fn include_whole_systems() -> &'static [&'static str] {
        &[]
    }
    /// Returns true only when this ValueSet can be treated as fully locally checkable.
    ///
    /// If `HAS_NONLOCAL_RULES` is true (filters, include.valueSet, or non-enumerated whole-system includes),
    /// local checks are best-effort and may require a terminology server.
    pub fn is_locally_enumerated() -> bool {
        !Self::HAS_NONLOCAL_RULES
            && (!Self::expansion_pairs().is_empty() || !Self::include_pairs().is_empty())
    }
    fn include_pairs() -> &'static [(&'static str, &'static str)] {
        &[
            ("http://hl7.org/fhir/research-study-status", "overall-study"),
            ("http://hl7.org/fhir/research-study-status", "active"),
            ("http://hl7.org/fhir/research-study-status", "active-but-not-recruiting"),
            ("http://hl7.org/fhir/research-study-status", "administratively-completed"),
            ("http://hl7.org/fhir/research-study-status", "approved"),
            ("http://hl7.org/fhir/research-study-status", "closed-to-accrual"),
            (
                "http://hl7.org/fhir/research-study-status",
                "closed-to-accrual-and-intervention",
            ),
            ("http://hl7.org/fhir/research-study-status", "completed"),
            ("http://hl7.org/fhir/research-study-status", "disapproved"),
            ("http://hl7.org/fhir/research-study-status", "enrolling-by-invitation"),
            ("http://hl7.org/fhir/research-study-status", "in-review"),
            ("http://hl7.org/fhir/research-study-status", "not-yet-recruiting"),
            ("http://hl7.org/fhir/research-study-status", "recruiting"),
            (
                "http://hl7.org/fhir/research-study-status",
                "temporarily-closed-to-accrual",
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "temporarily-closed-to-accrual-and-intervention",
            ),
            ("http://hl7.org/fhir/research-study-status", "terminated"),
            ("http://hl7.org/fhir/research-study-status", "withdrawn"),
        ]
    }
    fn include_entries() -> &'static [(
        &'static str,
        &'static str,
        Option<&'static str>,
        Option<&'static str>,
    )] {
        &[
            (
                "http://hl7.org/fhir/research-study-status",
                "active",
                Some("Active"),
                Some("Study is opened for accrual."),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "active-but-not-recruiting",
                Some("Active, not recruiting"),
                Some(
                    "The study is ongoing, and participants are receiving an intervention or being examined, but potential participants are not currently being recruited or enrolled.",
                ),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "administratively-completed",
                Some("Administratively Completed"),
                Some(
                    "Study is completed prematurely and will not resume; patients are no longer examined nor treated.",
                ),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "approved",
                Some("Approved"),
                Some("Protocol is approved by the review board."),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "closed-to-accrual",
                Some("Closed to Accrual"),
                Some(
                    "Study is closed for accrual; patients can be examined and treated.",
                ),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "closed-to-accrual-and-intervention",
                Some("Closed to Accrual and Intervention"),
                Some(
                    "Study is closed to accrual and intervention, i.e. the study is closed to enrollment, all study subjects have completed treatment or intervention but are still being followed according to the primary objective of the study.",
                ),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "completed",
                Some("Completed"),
                Some(
                    "The study closed according to the study plan. There will be no further treatments, interventions or data collection.",
                ),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "disapproved",
                Some("Disapproved"),
                Some("Protocol was disapproved by the review board."),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "enrolling-by-invitation",
                Some("Enrolling by invitation"),
                Some(
                    "The study is selecting its participants from a population, or group of people, decided on by the researchers in advance. These studies are not open to everyone who meets the eligibility criteria but only to people in that particular population, who are specifically invited to participate.",
                ),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "in-review",
                Some("In Review"),
                Some("Protocol is submitted to the review board for approval."),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "not-yet-recruiting",
                Some("Not yet recruiting"),
                Some("The study has not started recruiting participants."),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "overall-study",
                Some("Overall study"),
                Some(
                    "Used for documenting the start and end of the overall study, distinct from progress states.",
                ),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "recruiting",
                Some("Recruiting"),
                Some("The study is currently recruiting participants."),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "temporarily-closed-to-accrual",
                Some("Temporarily Closed to Accrual"),
                Some(
                    "Study is temporarily closed for accrual; can be potentially resumed in the future; patients can be examined and treated.",
                ),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "temporarily-closed-to-accrual-and-intervention",
                Some("Temporarily Closed to Accrual and Intervention"),
                Some(
                    "Study is temporarily closed for accrual and intervention and potentially can be resumed in the future.",
                ),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "terminated",
                Some("Terminated"),
                Some(
                    "The study has stopped early and will not start again. Participants are no longer being examined or treated.",
                ),
            ),
            (
                "http://hl7.org/fhir/research-study-status",
                "withdrawn",
                Some("Withdrawn"),
                Some("Protocol was withdrawn by the lead organization."),
            ),
        ]
    }
    /// Return the preferred display text for a (system, code) pair, if present in this ValueSet's explicit concepts.
    pub fn display(system: &str, code: &str) -> Option<&'static str> {
        for (s, c, d, _def) in Self::include_entries() {
            if *s == system && *c == code {
                return *d;
            }
        }
        None
    }
    /// Return the ValueSet concept definition for a (system, code) pair, if present.
    ///
    /// In `valuesets.json` this often comes from the extension:
    /// `http://hl7.org/fhir/StructureDefinition/valueset-concept-definition`.
    pub fn definition(system: &str, code: &str) -> Option<&'static str> {
        for (s, c, _d, def) in Self::include_entries() {
            if *s == system && *c == code {
                return *def;
            }
        }
        None
    }
    /// Create a minimal `Coding` from a (system, code) pair.
    ///
    /// If this ValueSet provides an explicit display for the code, it will be populated.
    pub fn to_coding(system: &str, code: &str) -> Coding {
        let sys_uri: Uri = Element {
            id: None,
            extension: None,
            value: Some(system.to_string()),
        };
        let code_el: Code = Element {
            id: None,
            extension: None,
            value: Some(code.to_string()),
        };
        let display = Self::display(system, code)
            .map(|s| FhirString {
                id: None,
                extension: None,
                value: Some(s.to_string()),
            });
        Coding {
            id: None,
            extension: None,
            system: Some(sys_uri),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(code_el),
            display,
            user_selected: None,
        }
    }
    fn exclude_pairs() -> &'static [(&'static str, &'static str)] {
        &[]
    }
    fn expansion_pairs() -> &'static [(&'static str, &'static str)] {
        &[]
    }
    /// Best-effort local validation for a `Coding` against this ValueSet.
    ///
    /// Rules:
    /// - Explicit excludes (compose.exclude.concept) always deny.
    /// - If expansion.contains exists, it is treated as an explicit allow-list.
    /// - Else if compose.include.concept exists, it is treated as an explicit allow-list.
    /// - Else: fall back to whole-system validation when possible (generated CodeSystem enums / RGB grammar).
    ///
    /// Note: terminology filters and include.valueSet references are NOT evaluated locally.
    pub fn contains_coding(coding: &Coding) -> bool {
        let system = match &coding.system {
            Some(s) => s.value.as_deref().unwrap_or(""),
            None => return false,
        };
        let code = match &coding.code {
            Some(c) => c.value.as_deref().unwrap_or(""),
            None => return false,
        };
        if Self::exclude_pairs().iter().any(|(s, c)| *s == system && *c == code) {
            return false;
        }
        let exp = Self::expansion_pairs();
        if !exp.is_empty() {
            return exp.iter().any(|(s, c)| *s == system && *c == code);
        }
        let inc = Self::include_pairs();
        if !inc.is_empty() {
            return inc.iter().any(|(s, c)| *s == system && *c == code);
        }
        match system {
            "http://hl7.org/fhir/research-study-status" => {
                super::super::code_systems::research_study_status::ResearchStudyStatus::try_from_code(
                        code,
                    )
                    .is_ok()
            }
            _ => false,
        }
    }
    /// Best-effort local validation for a `CodeableConcept` against this ValueSet.
    ///
    /// Returns true if any `coding` entry is contained in the ValueSet.
    pub fn contains_codeable_concept(cc: &CodeableConcept) -> bool {
        match cc.coding.as_ref() {
            Some(codings) => codings.iter().any(|c| Self::contains_coding(c)),
            None => false,
        }
    }
    /// Convenience: validate a FHIR `code` primitive when the binding is on `code`.
    ///
    /// This only works reliably when the ValueSet includes exactly one system.
    pub fn contains_code(code: &Code) -> bool {
        let code_str = match code.value.as_deref() {
            Some(v) => v,
            None => return false,
        };
        let systems = Self::include_systems();
        if systems.len() != 1 {
            return false;
        }
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(systems[0].to_string()),
        };
        let code: Code = Element {
            id: None,
            extension: None,
            value: Some(code_str.to_string()),
        };
        let coding = Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: None,
            code: Some(code),
            display: None,
            user_selected: None,
        };
        Self::contains_coding(&coding)
    }
    /// Create a minimal `CodeableConcept` (single coding) from a (system, code) pair.
    pub fn to_codeable_concept(system: &str, code: &str) -> CodeableConcept {
        let coding = Self::to_coding(system, code);
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: None,
        }
    }
}
impl super::super::bindings::ValueSetMembership<Code> for ResearchStudyStatus {
    fn contains(v: &Code) -> bool {
        Self::contains_code(v)
    }
}
impl super::super::bindings::ValueSetMembership<Coding> for ResearchStudyStatus {
    fn contains(v: &Coding) -> bool {
        Self::contains_coding(v)
    }
}
impl super::super::bindings::ValueSetMembership<CodeableConcept>
for ResearchStudyStatus {
    fn contains(v: &CodeableConcept) -> bool {
        Self::contains_codeable_concept(v)
    }
}
fn is_rgb_hex(code: &str) -> bool {
    let b = code.as_bytes();
    if b.len() != 7 || b[0] != b'#' {
        return false;
    }
    fn is_hex(x: u8) -> bool {
        (b'0'..=b'9').contains(&x) || (b'a'..=b'f').contains(&x)
            || (b'A'..=b'F').contains(&x)
    }
    is_hex(b[1]) && is_hex(b[2]) && is_hex(b[3]) && is_hex(b[4]) && is_hex(b[5])
        && is_hex(b[6])
}

