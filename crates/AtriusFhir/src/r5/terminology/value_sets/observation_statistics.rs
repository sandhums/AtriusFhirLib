// @generated by AtriusValueSetGenerator
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use super::super::super::{Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR ValueSet: StatisticsCode
///Canonical URL: http://hl7.org/fhir/ValueSet/observation-statistics
///Version: 5.0.0
///Title: Statistics Code
///Status: active
///Description Needed Here
///Compose includes 21 explicit concept codes
///Includes systems:
///- http://hl7.org/fhir/observation-statistics
pub struct StatisticsCode;
impl StatisticsCode {
    pub const URL: &'static str = "http://hl7.org/fhir/ValueSet/observation-statistics";
    pub const HAS_NONLOCAL_RULES: bool = false;
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    pub fn include_systems() -> &'static [&'static str] {
        &["http://hl7.org/fhir/observation-statistics"]
    }
    pub fn include_value_sets() -> &'static [&'static str] {
        &[]
    }
    /// Systems that are included as whole CodeSystems but are not locally enumerable.
    ///
    /// If this is non-empty, callers should use a terminology server for definitive validation.
    pub fn include_whole_systems() -> &'static [&'static str] {
        &[]
    }
    /// Returns true only when this ValueSet can be treated as fully locally checkable.
    ///
    /// If `HAS_NONLOCAL_RULES` is true (filters, include.valueSet, or non-enumerated whole-system includes),
    /// local checks are best-effort and may require a terminology server.
    pub fn is_locally_enumerated() -> bool {
        !Self::HAS_NONLOCAL_RULES
            && (!Self::expansion_pairs().is_empty() || !Self::include_pairs().is_empty())
    }
    fn include_pairs() -> &'static [(&'static str, &'static str)] {
        &[
            ("http://hl7.org/fhir/observation-statistics", "average"),
            ("http://hl7.org/fhir/observation-statistics", "maximum"),
            ("http://hl7.org/fhir/observation-statistics", "minimum"),
            ("http://hl7.org/fhir/observation-statistics", "count"),
            ("http://hl7.org/fhir/observation-statistics", "total-count"),
            ("http://hl7.org/fhir/observation-statistics", "median"),
            ("http://hl7.org/fhir/observation-statistics", "std-dev"),
            ("http://hl7.org/fhir/observation-statistics", "sum"),
            ("http://hl7.org/fhir/observation-statistics", "variance"),
            ("http://hl7.org/fhir/observation-statistics", "20-percent"),
            ("http://hl7.org/fhir/observation-statistics", "80-percent"),
            ("http://hl7.org/fhir/observation-statistics", "4-lower"),
            ("http://hl7.org/fhir/observation-statistics", "4-upper"),
            ("http://hl7.org/fhir/observation-statistics", "4-dev"),
            ("http://hl7.org/fhir/observation-statistics", "5-1"),
            ("http://hl7.org/fhir/observation-statistics", "5-2"),
            ("http://hl7.org/fhir/observation-statistics", "5-3"),
            ("http://hl7.org/fhir/observation-statistics", "5-4"),
            ("http://hl7.org/fhir/observation-statistics", "skew"),
            ("http://hl7.org/fhir/observation-statistics", "kurtosis"),
            ("http://hl7.org/fhir/observation-statistics", "regression"),
        ]
    }
    fn include_entries() -> &'static [(
        &'static str,
        &'static str,
        Option<&'static str>,
        Option<&'static str>,
    )] {
        &[
            (
                "http://hl7.org/fhir/observation-statistics",
                "20-percent",
                Some("20th Percentile"),
                Some(
                    "The 20th [Percentile](https://en.wikipedia.org/wiki/Percentile) of N measurements over the stated period.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "4-dev",
                Some("Quartile Deviation"),
                Some(
                    "The difference between the upper and lower [Quartiles](https://en.wikipedia.org/wiki/Quartile) is called the Interquartile range. (IQR = Q3-Q1) Quartile deviation or Semi-interquartile range is one-half the difference between the first and the third quartiles.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "4-lower",
                Some("Lower Quartile"),
                Some(
                    "The lower [Quartile](https://en.wikipedia.org/wiki/Quartile) Boundary of N measurements over the stated period.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "4-upper",
                Some("Upper Quartile"),
                Some(
                    "The upper [Quartile](https://en.wikipedia.org/wiki/Quartile) Boundary of N measurements over the stated period.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "5-1",
                Some("1st Quintile"),
                Some(
                    "The lowest of four values that divide the N measurements into a frequency distribution of five classes with each containing one fifth of the total population.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "5-2",
                Some("2nd Quintile"),
                Some(
                    "The second of four values that divide the N measurements into a frequency distribution of five classes with each containing one fifth of the total population.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "5-3",
                Some("3rd Quintile"),
                Some(
                    "The third of four values that divide the N measurements into a frequency distribution of five classes with each containing one fifth of the total population.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "5-4",
                Some("4th Quintile"),
                Some(
                    "The fourth of four values that divide the N measurements into a frequency distribution of five classes with each containing one fifth of the total population.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "80-percent",
                Some("80th Percentile"),
                Some(
                    "The 80th [Percentile](https://en.wikipedia.org/wiki/Percentile) of N measurements over the stated period.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "average",
                Some("Average"),
                Some(
                    "The [mean](https://en.wikipedia.org/wiki/Arithmetic_mean) of N measurements over the stated period.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "count",
                Some("Count"),
                Some(
                    "The [number] of valid measurements over the stated period that contributed to the other statistical outputs.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "kurtosis",
                Some("Kurtosis"),
                Some(
                    "Kurtosis  is a measure of the \"tailedness\" of the probability distribution of a real-valued random variable.   Source: [Wikipedia](https://en.wikipedia.org/wiki/Kurtosis).",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "maximum",
                Some("Maximum"),
                Some(
                    "The [maximum](https://en.wikipedia.org/wiki/Maximal_element) value of N measurements over the stated period.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "median",
                Some("Median"),
                Some(
                    "The [median](https://en.wikipedia.org/wiki/Median) of N measurements over the stated period.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "minimum",
                Some("Minimum"),
                Some(
                    "The [minimum](https://en.wikipedia.org/wiki/Minimal_element) value of N measurements over the stated period.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "regression",
                Some("Regression"),
                Some(
                    "Linear regression is an approach for modeling two-dimensional sample points with one independent variable and one dependent variable (conventionally, the x and y coordinates in a Cartesian coordinate system) and finds a linear function (a non-vertical straight line) that, as accurately as possible, predicts the dependent variable values as a function of the independent variables. Source: [Wikipedia](https://en.wikipedia.org/wiki/Simple_linear_regression)  This Statistic code will return both a gradient and an intercept value.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "skew",
                Some("Skew"),
                Some(
                    "Skewness is a measure of the asymmetry of the probability distribution of a real-valued random variable about its mean. The skewness value can be positive or negative, or even undefined.  Source: [Wikipedia](https://en.wikipedia.org/wiki/Skewness).",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "std-dev",
                Some("Standard Deviation"),
                Some(
                    "The [standard deviation](https://en.wikipedia.org/wiki/Standard_deviation) of N measurements over the stated period.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "sum",
                Some("Sum"),
                Some(
                    "The [sum](https://en.wikipedia.org/wiki/Summation) of N measurements over the stated period.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "total-count",
                Some("Total Count"),
                Some(
                    "The total [number] of valid measurements over the stated period, including observations that were ignored because they did not contain valid result values.",
                ),
            ),
            (
                "http://hl7.org/fhir/observation-statistics",
                "variance",
                Some("Variance"),
                Some(
                    "The [variance](https://en.wikipedia.org/wiki/Variance) of N measurements over the stated period.",
                ),
            ),
        ]
    }
    /// Return the preferred display text for a (system, code) pair, if present in this ValueSet's explicit concepts.
    pub fn display(system: &str, code: &str) -> Option<&'static str> {
        for (s, c, d, _def) in Self::include_entries() {
            if *s == system && *c == code {
                return *d;
            }
        }
        None
    }
    /// Return the ValueSet concept definition for a (system, code) pair, if present.
    ///
    /// In `valuesets.json` this often comes from the extension:
    /// `http://hl7.org/fhir/StructureDefinition/valueset-concept-definition`.
    pub fn definition(system: &str, code: &str) -> Option<&'static str> {
        for (s, c, _d, def) in Self::include_entries() {
            if *s == system && *c == code {
                return *def;
            }
        }
        None
    }
    /// Create a minimal `Coding` from a (system, code) pair.
    ///
    /// If this ValueSet provides an explicit display for the code, it will be populated.
    pub fn to_coding(system: &str, code: &str) -> Coding {
        let sys_uri: Uri = Element {
            id: None,
            extension: None,
            value: Some(system.to_string()),
        };
        let code_el: Code = Element {
            id: None,
            extension: None,
            value: Some(code.to_string()),
        };
        let display = Self::display(system, code)
            .map(|s| FhirString {
                id: None,
                extension: None,
                value: Some(s.to_string()),
            });
        Coding {
            id: None,
            extension: None,
            system: Some(sys_uri),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(code_el),
            display,
            user_selected: None,
        }
    }
    fn exclude_pairs() -> &'static [(&'static str, &'static str)] {
        &[]
    }
    fn expansion_pairs() -> &'static [(&'static str, &'static str)] {
        &[]
    }
    /// Best-effort local validation for a `Coding` against this ValueSet.
    ///
    /// Rules:
    /// - Explicit excludes (compose.exclude.concept) always deny.
    /// - If expansion.contains exists, it is treated as an explicit allow-list.
    /// - Else if compose.include.concept exists, it is treated as an explicit allow-list.
    /// - Else: fall back to whole-system validation when possible (generated CodeSystem enums / RGB grammar).
    ///
    /// Note: terminology filters and include.valueSet references are NOT evaluated locally.
    pub fn contains_coding(coding: &Coding) -> bool {
        let system = match &coding.system {
            Some(s) => s.value.as_deref().unwrap_or(""),
            None => return false,
        };
        let code = match &coding.code {
            Some(c) => c.value.as_deref().unwrap_or(""),
            None => return false,
        };
        if Self::exclude_pairs().iter().any(|(s, c)| *s == system && *c == code) {
            return false;
        }
        let exp = Self::expansion_pairs();
        if !exp.is_empty() {
            return exp.iter().any(|(s, c)| *s == system && *c == code);
        }
        let inc = Self::include_pairs();
        if !inc.is_empty() {
            return inc.iter().any(|(s, c)| *s == system && *c == code);
        }
        match system {
            "http://hl7.org/fhir/observation-statistics" => {
                super::super::code_systems::observation_statistics::StatisticsCode::try_from_code(
                        code,
                    )
                    .is_ok()
            }
            _ => false,
        }
    }
    /// Best-effort local validation for a `CodeableConcept` against this ValueSet.
    ///
    /// Returns true if any `coding` entry is contained in the ValueSet.
    pub fn contains_codeable_concept(cc: &CodeableConcept) -> bool {
        match cc.coding.as_ref() {
            Some(codings) => codings.iter().any(|c| Self::contains_coding(c)),
            None => false,
        }
    }
    /// Convenience: validate a FHIR `code` primitive when the binding is on `code`.
    ///
    /// This only works reliably when the ValueSet includes exactly one system.
    pub fn contains_code(code: &Code) -> bool {
        let code_str = match code.value.as_deref() {
            Some(v) => v,
            None => return false,
        };
        let systems = Self::include_systems();
        if systems.len() != 1 {
            return false;
        }
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(systems[0].to_string()),
        };
        let code: Code = Element {
            id: None,
            extension: None,
            value: Some(code_str.to_string()),
        };
        let coding = Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: None,
            code: Some(code),
            display: None,
            user_selected: None,
        };
        Self::contains_coding(&coding)
    }
    /// Create a minimal `CodeableConcept` (single coding) from a (system, code) pair.
    pub fn to_codeable_concept(system: &str, code: &str) -> CodeableConcept {
        let coding = Self::to_coding(system, code);
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: None,
        }
    }
}
impl super::super::bindings::ValueSetMembership<Code> for StatisticsCode {
    fn contains(v: &Code) -> bool {
        Self::contains_code(v)
    }
}
impl super::super::bindings::ValueSetMembership<Coding> for StatisticsCode {
    fn contains(v: &Coding) -> bool {
        Self::contains_coding(v)
    }
}
impl super::super::bindings::ValueSetMembership<CodeableConcept> for StatisticsCode {
    fn contains(v: &CodeableConcept) -> bool {
        Self::contains_codeable_concept(v)
    }
}
fn is_rgb_hex(code: &str) -> bool {
    let b = code.as_bytes();
    if b.len() != 7 || b[0] != b'#' {
        return false;
    }
    fn is_hex(x: u8) -> bool {
        (b'0'..=b'9').contains(&x) || (b'a'..=b'f').contains(&x)
            || (b'A'..=b'F').contains(&x)
    }
    is_hex(b[1]) && is_hex(b[2]) && is_hex(b[3]) && is_hex(b[4]) && is_hex(b[5])
        && is_hex(b[6])
}

