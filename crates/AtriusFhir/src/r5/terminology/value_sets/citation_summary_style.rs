// @generated by AtriusValueSetGenerator
// DO NOT EDIT MANUALLY

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(clippy::upper_case_acronyms)]

use super::super::super::{Code, CodeableConcept, Coding, Element, Uri};

use super::super::super::string::String as FhirString;

///FHIR ValueSet: CitationSummaryStyle
///Canonical URL: http://hl7.org/fhir/ValueSet/citation-summary-style
///Version: 5.0.0
///Title: Citation Summary Style
///Status: draft
///The format for display of the citation.
///Compose includes 14 explicit concept codes
///Includes systems:
///- http://hl7.org/fhir/citation-summary-style
pub struct CitationSummaryStyle;
impl CitationSummaryStyle {
    pub const URL: &'static str = "http://hl7.org/fhir/ValueSet/citation-summary-style";
    pub const HAS_NONLOCAL_RULES: bool = false;
    pub fn version() -> Option<&'static str> {
        Some("5.0.0")
    }
    pub fn include_systems() -> &'static [&'static str] {
        &["http://hl7.org/fhir/citation-summary-style"]
    }
    pub fn include_value_sets() -> &'static [&'static str] {
        &[]
    }
    /// Systems that are included as whole CodeSystems but are not locally enumerable.
    ///
    /// If this is non-empty, callers should use a terminology server for definitive validation.
    pub fn include_whole_systems() -> &'static [&'static str] {
        &[]
    }
    /// Returns true only when this ValueSet can be treated as fully locally checkable.
    ///
    /// If `HAS_NONLOCAL_RULES` is true (filters, include.valueSet, or non-enumerated whole-system includes),
    /// local checks are best-effort and may require a terminology server.
    pub fn is_locally_enumerated() -> bool {
        !Self::HAS_NONLOCAL_RULES
            && (!Self::expansion_pairs().is_empty() || !Self::include_pairs().is_empty())
    }
    fn include_pairs() -> &'static [(&'static str, &'static str)] {
        &[
            ("http://hl7.org/fhir/citation-summary-style", "vancouver"),
            ("http://hl7.org/fhir/citation-summary-style", "ama11"),
            ("http://hl7.org/fhir/citation-summary-style", "apa7"),
            ("http://hl7.org/fhir/citation-summary-style", "apa6"),
            ("http://hl7.org/fhir/citation-summary-style", "asa6"),
            ("http://hl7.org/fhir/citation-summary-style", "mla8"),
            ("http://hl7.org/fhir/citation-summary-style", "cochrane"),
            ("http://hl7.org/fhir/citation-summary-style", "elsevier-harvard"),
            ("http://hl7.org/fhir/citation-summary-style", "nature"),
            ("http://hl7.org/fhir/citation-summary-style", "acs"),
            ("http://hl7.org/fhir/citation-summary-style", "chicago-a-17"),
            ("http://hl7.org/fhir/citation-summary-style", "chicago-b-17"),
            ("http://hl7.org/fhir/citation-summary-style", "ieee"),
            ("http://hl7.org/fhir/citation-summary-style", "comppub"),
        ]
    }
    fn include_entries() -> &'static [(
        &'static str,
        &'static str,
        Option<&'static str>,
        Option<&'static str>,
    )] {
        &[
            (
                "http://hl7.org/fhir/citation-summary-style",
                "acs",
                Some("American Chemical Society"),
                Some("Chemistry."),
            ),
            (
                "http://hl7.org/fhir/citation-summary-style",
                "ama11",
                Some("American Medical Association 11th edition"),
                Some("Medicine."),
            ),
            (
                "http://hl7.org/fhir/citation-summary-style",
                "apa6",
                Some("American Psychological Association 6th edition"),
                Some("APA 6th edition used by Education, Psychology and Sciences."),
            ),
            (
                "http://hl7.org/fhir/citation-summary-style",
                "apa7",
                Some("American Psychological Association 7th edition"),
                Some(
                    "APA 7th edition (October 2019) used by Education, Psychology and Sciences.",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-summary-style",
                "asa6",
                Some("American Sociological Association 6th edition"),
                Some("Sociology."),
            ),
            (
                "http://hl7.org/fhir/citation-summary-style",
                "chicago-a-17",
                Some("Chicago Style Version 17 Author Date"),
                Some("Used by Business, History and the Fine Arts."),
            ),
            (
                "http://hl7.org/fhir/citation-summary-style",
                "chicago-b-17",
                Some("Chicago Style Version 17 Full note"),
                Some("Used by Business, History and the Fine Arts."),
            ),
            (
                "http://hl7.org/fhir/citation-summary-style",
                "cochrane",
                Some("Cochrane Style"),
                Some(
                    "Used by Cochrane reviews, example: Huang, M, Tang T, Pang, P, Li, M, Ma R, Lu, J, et al. Treating COVID-19 with Chloroquine. J Mol Cell Biol 2020; 12(4):322–5.",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-summary-style",
                "comppub",
                Some("Computable Publishing"),
                Some(
                    "Citation style to report human-readable and/or machine-readable access paths, example: Alper BS, Dehnbostel J, Shahin K. 14-day mortality remdesivir vs placebo meta-analysis (ACTT-1, Wang et al, WHO SOLIDARITY).  Fast Evidence Interoperability Resources (FEvIR) Platform, entry 55, version 4. Created 2020 Dec 17. Revised 2020 Dec 21. Accessed 2021 Mar 13. Computable resource at: https://fevir.net/resources/Evidence/55.",
                ),
            ),
            (
                "http://hl7.org/fhir/citation-summary-style",
                "elsevier-harvard",
                Some("Elsevier-Harvard Style"),
                Some("Biology ecology."),
            ),
            (
                "http://hl7.org/fhir/citation-summary-style",
                "ieee",
                Some("Institute of Electrical and Electronics Engineers"),
                Some("IT, Engineering."),
            ),
            (
                "http://hl7.org/fhir/citation-summary-style",
                "mla8",
                Some("Modern Language Association 8th edition"),
                Some("MLA 8th edition used by the Humanities."),
            ),
            (
                "http://hl7.org/fhir/citation-summary-style",
                "nature",
                Some("Nature Referencing style"),
                Some("Biology."),
            ),
            (
                "http://hl7.org/fhir/citation-summary-style",
                "vancouver",
                Some("Vancouver style"),
                Some(
                    "Citation style used by International Committee of Medical Journal Editors and maintained by US National Library of Medicine.",
                ),
            ),
        ]
    }
    /// Return the preferred display text for a (system, code) pair, if present in this ValueSet's explicit concepts.
    pub fn display(system: &str, code: &str) -> Option<&'static str> {
        for (s, c, d, _def) in Self::include_entries() {
            if *s == system && *c == code {
                return *d;
            }
        }
        None
    }
    /// Return the ValueSet concept definition for a (system, code) pair, if present.
    ///
    /// In `valuesets.json` this often comes from the extension:
    /// `http://hl7.org/fhir/StructureDefinition/valueset-concept-definition`.
    pub fn definition(system: &str, code: &str) -> Option<&'static str> {
        for (s, c, _d, def) in Self::include_entries() {
            if *s == system && *c == code {
                return *def;
            }
        }
        None
    }
    /// Create a minimal `Coding` from a (system, code) pair.
    ///
    /// If this ValueSet provides an explicit display for the code, it will be populated.
    pub fn to_coding(system: &str, code: &str) -> Coding {
        let sys_uri: Uri = Element {
            id: None,
            extension: None,
            value: Some(system.to_string()),
        };
        let code_el: Code = Element {
            id: None,
            extension: None,
            value: Some(code.to_string()),
        };
        let display = Self::display(system, code)
            .map(|s| FhirString {
                id: None,
                extension: None,
                value: Some(s.to_string()),
            });
        Coding {
            id: None,
            extension: None,
            system: Some(sys_uri),
            version: Self::version()
                .map(|v| FhirString {
                    id: None,
                    extension: None,
                    value: Some(v.to_string()),
                }),
            code: Some(code_el),
            display,
            user_selected: None,
        }
    }
    fn exclude_pairs() -> &'static [(&'static str, &'static str)] {
        &[]
    }
    fn expansion_pairs() -> &'static [(&'static str, &'static str)] {
        &[]
    }
    /// Best-effort local validation for a `Coding` against this ValueSet.
    ///
    /// Rules:
    /// - Explicit excludes (compose.exclude.concept) always deny.
    /// - If expansion.contains exists, it is treated as an explicit allow-list.
    /// - Else if compose.include.concept exists, it is treated as an explicit allow-list.
    /// - Else: fall back to whole-system validation when possible (generated CodeSystem enums / RGB grammar).
    ///
    /// Note: terminology filters and include.valueSet references are NOT evaluated locally.
    pub fn contains_coding(coding: &Coding) -> bool {
        let system = match &coding.system {
            Some(s) => s.value.as_deref().unwrap_or(""),
            None => return false,
        };
        let code = match &coding.code {
            Some(c) => c.value.as_deref().unwrap_or(""),
            None => return false,
        };
        if Self::exclude_pairs().iter().any(|(s, c)| *s == system && *c == code) {
            return false;
        }
        let exp = Self::expansion_pairs();
        if !exp.is_empty() {
            return exp.iter().any(|(s, c)| *s == system && *c == code);
        }
        let inc = Self::include_pairs();
        if !inc.is_empty() {
            return inc.iter().any(|(s, c)| *s == system && *c == code);
        }
        match system {
            "http://hl7.org/fhir/citation-summary-style" => {
                super::super::code_systems::citation_summary_style::CitationSummaryStyle::try_from_code(
                        code,
                    )
                    .is_ok()
            }
            _ => false,
        }
    }
    /// Best-effort local validation for a `CodeableConcept` against this ValueSet.
    ///
    /// Returns true if any `coding` entry is contained in the ValueSet.
    pub fn contains_codeable_concept(cc: &CodeableConcept) -> bool {
        match cc.coding.as_ref() {
            Some(codings) => codings.iter().any(|c| Self::contains_coding(c)),
            None => false,
        }
    }
    /// Convenience: validate a FHIR `code` primitive when the binding is on `code`.
    ///
    /// This only works reliably when the ValueSet includes exactly one system.
    pub fn contains_code(code: &Code) -> bool {
        let code_str = match code.value.as_deref() {
            Some(v) => v,
            None => return false,
        };
        let systems = Self::include_systems();
        if systems.len() != 1 {
            return false;
        }
        let system: Uri = Element {
            id: None,
            extension: None,
            value: Some(systems[0].to_string()),
        };
        let code: Code = Element {
            id: None,
            extension: None,
            value: Some(code_str.to_string()),
        };
        let coding = Coding {
            id: None,
            extension: None,
            system: Some(system),
            version: None,
            code: Some(code),
            display: None,
            user_selected: None,
        };
        Self::contains_coding(&coding)
    }
    /// Create a minimal `CodeableConcept` (single coding) from a (system, code) pair.
    pub fn to_codeable_concept(system: &str, code: &str) -> CodeableConcept {
        let coding = Self::to_coding(system, code);
        CodeableConcept {
            id: None,
            extension: None,
            coding: Some(vec![coding]),
            text: None,
        }
    }
}
impl super::super::bindings::ValueSetMembership<Code> for CitationSummaryStyle {
    fn contains(v: &Code) -> bool {
        Self::contains_code(v)
    }
}
impl super::super::bindings::ValueSetMembership<Coding> for CitationSummaryStyle {
    fn contains(v: &Coding) -> bool {
        Self::contains_coding(v)
    }
}
impl super::super::bindings::ValueSetMembership<CodeableConcept>
for CitationSummaryStyle {
    fn contains(v: &CodeableConcept) -> bool {
        Self::contains_codeable_concept(v)
    }
}
fn is_rgb_hex(code: &str) -> bool {
    let b = code.as_bytes();
    if b.len() != 7 || b[0] != b'#' {
        return false;
    }
    fn is_hex(x: u8) -> bool {
        (b'0'..=b'9').contains(&x) || (b'a'..=b'f').contains(&x)
            || (b'A'..=b'F').contains(&x)
    }
    is_hex(b[1]) && is_hex(b[2]) && is_hex(b[3]) && is_hex(b[4]) && is_hex(b[5])
        && is_hex(b[6])
}

